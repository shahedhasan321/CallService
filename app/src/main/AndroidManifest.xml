<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.callservice">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/call"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".eticketingactivity"></activity>
        <activity android:name=".transactionactivity" />
        <activity android:name=".operatoractivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".trainactivity" />
        <activity android:name=".teletalkfirstactivity" />
        <activity android:name=".airtelfirstactivity" />
        <activity android:name=".robifirstactivity" />
        <activity android:name=".banglalinkfirstactivity" />
        <activity android:name=".gpfirstactivity" />
        <activity
            android:name=".splash"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>